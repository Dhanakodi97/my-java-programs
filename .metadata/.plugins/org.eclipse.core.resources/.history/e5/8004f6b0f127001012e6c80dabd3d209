package igpWebScrapping;

import java.io.File;
import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.IOException;
import java.time.Duration;
import java.util.List;

import org.apache.poi.ss.usermodel.Row;
import org.apache.poi.xssf.usermodel.XSSFSheet;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class IGPChatGpt {

    private static int j;

	public static void main(String[] args) throws IOException {
        WebDriver driver = new ChromeDriver();
        driver.manage().window().maximize();

        try {
            FileInputStream file = new FileInputStream(new File(".\\Input\\IGPInput.xlsx"));
            XSSFWorkbook workbook = new XSSFWorkbook(file);
            XSSFSheet sheet = workbook.getSheetAt(0);
            Row headerRow = sheet.createRow(0);
            headerRow.createCell(0).setCellValue("Product URL");
    		headerRow.createCell(1).setCellValue("Product Name");
    		headerRow.createCell(2).setCellValue("MRP");
    		headerRow.createCell(3).setCellValue("SP");		
    		headerRow.createCell(4).setCellValue("Offer");
            
            System.out.println("Reading input data from Excel...");

            for (int i = 1; i <= sheet.getLastRowNum(); i++) {
                Row row = sheet.getRow(i);
                String searchKey = row.getCell(0).getStringCellValue();
                System.out.println("\nSearching for: " + searchKey);

                driver.get("https://www.igp.com/");
                WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(10));

                // Search product
                WebElement searchBar = wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//input[@class='search-bar m-t-5 p-l-10 p-r-10 morphsearch-input']")));
                searchBar.clear();
                searchBar.sendKeys(searchKey);
                driver.findElement(By.xpath("//div[@class='icon-wrapper search-icon-wrapper']")).click();

                // Wait for products to load
                wait.until(ExpectedConditions.presenceOfAllElementsLocatedBy(By.xpath("//a[@class='image-holder']")));
                List<WebElement> products = driver.findElements(By.xpath("//div[@class='product-info-container']"));

                //int count = Math.min(products.size(), 3); // limit to 3 items per search
//                int rowNum = 1;
//        		List<WebElement> items = driver.findElements(By.xpath("//a[@class='image-holder']"));
//        		for (int j = 0; i <=sheet.getLastRowNum(); i++) {
//        			WebElement item = items.get(i); 
//        			Row row1 = sheet.createRow(rowNum++);
//        			// Click the product to go to the product page
//        			//item.click();
//                //for (int j = 0; j < count; j++) {
//                    try {
//                        WebElement product = products.get(j);
//                        
//                     // Getting Product Name
//            			WebElement titleElement = wait.until(ExpectedConditions.presenceOfElementLocated(By.xpath("//h1[@class='prod-name Heading-5--Bold ']")));
//            			String productTitle = titleElement.getText();
//            			row.createCell(1).setCellValue(productTitle);
//            			System.out.println("Product Title: " + productTitle);
//            			// Getting Product Name
//            			WebElement mrp = wait.until(ExpectedConditions.presenceOfElementLocated(By.xpath("//span[@class='striked-price Paragraph-16-M--Regular strikethrough ']")));
//            			String productMrp = mrp.getText();
//            			row.createCell(1).setCellValue(productMrp);
//            			System.out.println("Product Title: " + productMrp);	
//            			// Getting Product Name
//            			WebElement sellingPrice = wait.until(ExpectedConditions.presenceOfElementLocated(By.xpath("//div[@class='prod-price Paragraph-24-2XL--Semibold  ']")));
//            			String productSellPrice = sellingPrice.getText();
//            			row.createCell(1).setCellValue(productSellPrice);
//            			System.out.println("Product Title: " + productSellPrice);		
//
//                        //String name = product.findElement(By.xpath("//h1[@class='prod-name Heading-5--Bold ']")).getText();
//                        //String mrp = product.findElement(By.xpath("//span[@class='striked-price Paragraph-16-M--Regular strikethrough ']")).getText(); // MRP
//                        //String price = product.findElement(By.xpath("//div[@class='prod-price Paragraph-24-2XL--Semibold  ']")).getText(); // Discounted price
//
//                        String offer = "";
//                        List<WebElement> offerElement = product.findElements(By.xpath("//span[@class='discount-percent Paragraph-12-XS--Semibold']"));
//                        if (!offerElement.isEmpty()) {
//                            offer = offerElement.get(0).getText();
                
                
             // ... load Excel as before

                int rowNum = 1;

                for (int j = 1; j <= sheet.getLastRowNum(); j++) {
                    Row row1 = sheet.getRow(i);
                    String searchUrl = row1.getCell(0).getStringCellValue();
                    System.out.println("\nOpening product page: " + searchUrl);
                    
                    driver.get(searchUrl);
                    WebDriverWait wait1 = new WebDriverWait(driver, Duration.ofSeconds(10));
                    
                    try {
                        WebElement titleElement = wait1.until(ExpectedConditions.presenceOfElementLocated(By.xpath("//h1[@class='prod-name Heading-5--Bold ']")));
                        String productTitle = titleElement.getText();

                        WebElement mrpElement = wait1.until(ExpectedConditions.presenceOfElementLocated(By.xpath("//span[contains(@class, 'striked-price')]")));
                        String productMrp = mrpElement.getText();

                        WebElement sellingPriceElement = wait1.until(ExpectedConditions.presenceOfElementLocated(By.xpath("//div[contains(@class, 'prod-price')]")));
                        String productSellPrice = sellingPriceElement.getText();

                        String offer = "";
                        List<WebElement> offerElements = driver.findElements(By.xpath("//span[contains(@class, 'discount-percent')]"));
                        if (!offerElements.isEmpty()) {
                            offer = offerElements.get(0).getText();
                        }

                        Row newRow = sheet.createRow(rowNum++);
                        newRow.createCell(0).setCellValue(searchUrl);
                        newRow.createCell(1).setCellValue(productTitle);
                        newRow.createCell(2).setCellValue(productMrp);
                        newRow.createCell(3).setCellValue(productSellPrice);
                        newRow.createCell(4).setCellValue(offer);

                        System.out.println("Success: " + productTitle);
                        
                    } catch (Exception e) {
                        System.out.println("Failed to extract product at: " + searchUrl);
                    }
                }

                // Save workbook
                FileOutputStream outFile = new FileOutputStream(new File(".\\Output\\IGPOutput.xlsx"));
                workbook.write(outFile);
                outFile.close();
                workbook.close();

                        }

//                        System.out.println("Product " + (j + 1) + ":");
//                        System.out.println("  Name: " + name);
//                        System.out.println("  MRP: " + mrp);
//                        System.out.println("  Selling Price: " + price);
//                        System.out.println("  Offer: " + offer);
                    } catch (Exception e) {
                        System.out.println("  Error reading product " + (j + 1));
                    
                }
            

//            workbook.close();
//            file.close();
//
//        } catch (Exception e) {
//            e.printStackTrace();
//        }

        driver.quit();
    }
}
